diff --git a/examples/simple_repeater/MyMesh.cpp b/examples/simple_repeater/MyMesh.cpp
index 24e8894..7656271 100644
--- a/examples/simple_repeater/MyMesh.cpp
+++ b/examples/simple_repeater/MyMesh.cpp
@@ -926,6 +926,10 @@ void MyMesh::begin(FILESYSTEM *fs) {
   updateAdvertTimer();
   updateFloodAdvertTimer();
 
+#ifdef MOTION_ALERT_ENABLED
+  initMotionChannel();
+#endif
+
   board.setAdcMultiplier(_prefs.adc_multiplier);
 
 #if ENV_INCLUDE_GPS == 1
@@ -1297,6 +1301,10 @@ void MyMesh::loop() {
 
   mesh::Mesh::loop();
 
+#ifdef MOTION_ALERT_ENABLED
+  checkMotion();
+#endif
+
   if (next_flood_advert && millisHasNowPassed(next_flood_advert)) {
     mesh::Packet *pkt = createSelfAdvert();
     uint32_t delay_millis = 0;
@@ -1335,6 +1343,58 @@ void MyMesh::loop() {
   last_millis = now;
 }
 
+#ifdef MOTION_ALERT_ENABLED
+void MyMesh::initMotionChannel() {
+  // Private channel: use explicit key from MOTION_CHANNEL_KEY
+  const uint8_t key[] = MOTION_CHANNEL_KEY;
+  memset(_motion_channel.secret, 0, PUB_KEY_SIZE);
+  memcpy(_motion_channel.secret, key, 16);
+  // Channel hash = SHA256(secret)[0..PATH_HASH_SIZE]
+  mesh::Utils::sha256(_motion_channel.hash, PATH_HASH_SIZE, _motion_channel.secret, 16);
+  _motion_cooldown = 0;
+  _motion_triggered = false;
+  pinMode(MOTION_PIN, INPUT);
+  MESH_DEBUG_PRINTLN("Motion alert enabled on pin %d, channel: %s", MOTION_PIN, MOTION_CHANNEL_NAME);
+}
+
+void MyMesh::sendMotionAlert() {
+  uint32_t timestamp = getRTCClock()->getCurrentTime();
+  uint8_t temp[5 + 80];
+  memcpy(temp, &timestamp, 4);
+  temp[4] = TXT_TYPE_PLAIN;
+  char msg[64];
+  snprintf(msg, sizeof(msg), "%s: Motion detected", _prefs.node_name);
+  int msg_len = strlen(msg);
+  memcpy(&temp[5], msg, msg_len);
+
+  auto pkt = createGroupDatagram(PAYLOAD_TYPE_GRP_TXT, _motion_channel, temp, 5 + msg_len);
+  if (pkt) {
+    sendFlood(pkt);
+    MESH_DEBUG_PRINTLN("Motion alert sent to %s", MOTION_CHANNEL_NAME);
+  }
+}
+
+void MyMesh::checkMotion() {
+  if (_motion_cooldown && !millisHasNowPassed(_motion_cooldown)) {
+    // Still in cooldown -- but check if motion has ended to reset trigger
+    int val = analogRead(MOTION_PIN);
+    if (val < MOTION_THRESHOLD) {
+      _motion_triggered = false;
+    }
+    return;
+  }
+
+  int val = analogRead(MOTION_PIN);
+  if (val >= MOTION_THRESHOLD && !_motion_triggered) {
+    _motion_triggered = true;
+    sendMotionAlert();
+    _motion_cooldown = futureMillis(MOTION_COOLDOWN_MS);
+  } else if (val < MOTION_THRESHOLD) {
+    _motion_triggered = false;
+  }
+}
+#endif
+
 // To check if there is pending work
 bool MyMesh::hasPendingWork() const {
 #if defined(WITH_BRIDGE)
diff --git a/examples/simple_repeater/MyMesh.h b/examples/simple_repeater/MyMesh.h
index 9295844..5c08264 100644
--- a/examples/simple_repeater/MyMesh.h
+++ b/examples/simple_repeater/MyMesh.h
@@ -35,6 +35,26 @@
 #include <helpers/RegionMap.h>
 #include "RateLimiter.h"
 
+// --- Motion detection config ---
+#ifdef MOTION_ALERT_ENABLED
+  #ifndef MOTION_PIN
+    #define MOTION_PIN WB_A1  // RAK19007 AIN1 -> nRF52840 P0.31
+  #endif
+  #ifndef MOTION_THRESHOLD
+    #define MOTION_THRESHOLD 100  // ADC threshold (10-bit: ~0.35V of 3.6V range)
+  #endif
+  #ifndef MOTION_COOLDOWN_MS
+    #define MOTION_COOLDOWN_MS 60000  // 60s cooldown between alerts
+  #endif
+  #ifndef MOTION_CHANNEL_NAME
+    #define MOTION_CHANNEL_NAME "fudge"
+  #endif
+  #ifndef MOTION_CHANNEL_KEY
+    #define MOTION_CHANNEL_KEY { 0x3d, 0xb6, 0x96, 0x3c, 0xfd, 0x7d, 0x18, 0x21, 0x66, 0xc9, 0xab, 0x7f, 0x3e, 0x91, 0x87, 0x50 }
+  #endif
+#endif
+// --- End motion detection config ---
+
 #ifdef WITH_BRIDGE
 extern AbstractBridge* bridge;
 #endif
@@ -118,6 +138,15 @@ class MyMesh : public mesh::Mesh, public CommonCLICallbacks {
   ESPNowBridge bridge;
 #endif
 
+#ifdef MOTION_ALERT_ENABLED
+  mesh::GroupChannel _motion_channel;
+  unsigned long _motion_cooldown;
+  bool _motion_triggered;
+  void initMotionChannel();
+  void checkMotion();
+  void sendMotionAlert();
+#endif
+
   void putNeighbour(const mesh::Identity& id, uint32_t timestamp, float snr);
   void sendNodeDiscoverReq();
   uint8_t handleLoginReq(const mesh::Identity& sender, const uint8_t* secret, uint32_t sender_timestamp, const uint8_t* data, bool is_flood);
